/*
 * File Translator.h
 */
 #ifndef TRANSLATOR_H
 #define TRANSLATOR_H

/******************LETTERS AND NUMBERS******************/
//#define A='.-'

/*
 class Translation
 {
 		public:
 			Translation();
 			Translation( const std::string &user_input,
 						 const std::string &translated);
 			virtual ~Translation();

		//Getters
		const std::string& getInput(); const;

		private:
			std::string user_input;
			std::string translated;

 }; */

 /*
  * Translation algorithm step-by-step
  * 1. Input string, i.e. "Python"
  * 2. Transform to uppercase, "PYTHON"
  * 3. Translate one letter at a time: P, Y, T, H, O, N
  * 4. Save translated morse to a variable, i.e. string morse
  * 5. Move translated morse to print and output to a file
  */

 #endif // TRANSLATOR_H